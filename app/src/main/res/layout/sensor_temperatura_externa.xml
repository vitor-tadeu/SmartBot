<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.sensores.TemperaturaExterna">

    <TextView
        android:id="@+id/txtSensorTemperaturaExterna"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbarTemperaturaExterna"
        tools:text="TemperaturaExterna" />

    <include
        android:id="@+id/toolbarTemperaturaExterna"
        layout="@layout/toolbar" />
</android.support.constraint.ConstraintLayout>